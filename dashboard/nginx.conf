server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes (e.g. /requests/:id) resolve to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
